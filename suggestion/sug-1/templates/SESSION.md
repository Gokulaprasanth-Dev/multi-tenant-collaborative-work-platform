# SESSION.md
<!-- Auto-maintained by Claude. Updated every 5 turns or after every commit. Never delete. -->

## Last Updated
[YYYY-MM-DD HH:MM] | Turn [N]

## Active Task
<!-- One sentence — exact file and line range if mid-edit -->
[e.g. "Implementing rate limiting in src/shared/auth-middleware/rate-limit.ts"]

## State
<!-- IN_PROGRESS | BLOCKED | BROKEN | NEEDS_REVIEW | DONE -->
IN_PROGRESS

## Resumption Point
<!-- The exact next action — specific enough that cold-start Claude acts without reading other files -->
[e.g. "Write integration test for rate limit — tests/integration/rateLimit.test.ts — use existing auth test as template"]

## What's Done This Session
<!-- Append only — never overwrite history -->
- [ ] [completed action + file changed]

## Broken / Unstable Right Now
<!-- Anything currently broken, even if unrelated to current task -->
[e.g. "npm run test:unit fails — jest config issue, not related to current task — do not touch"]

## Last Decision Made
<!-- Most recent architectural or approach decision — prevents re-debating on resume -->
[e.g. "Using sliding window (Redis ZADD) over fixed window — simpler, already have Redis"]

## Context That Would Be Lost
<!-- What Claude knows NOW that won't be obvious from reading files -->
[e.g. "Outbox poller timing issue only reproduces under load — normal tests pass fine"]

## Open Questions (Need User Input)
- [e.g. "Rate limit: per-user or per-org? Blocked until decided."]
